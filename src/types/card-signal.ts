/**
 * src/types/card-signal.ts — public type re-export for X12.
 *
 * Cards and consumers import types from here; the implementation
 * lives in `src/core/card-signal-protocol.ts`.
 *
 * Sprint 375 / v13.39.0.
 */

export type { CardSignal } from "../core/card-signal-protocol";
