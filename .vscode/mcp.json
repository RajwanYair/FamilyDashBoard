{
    "servers": {
        "github": {
            "type": "http",
            "url": "https://api.githubcopilot.com/mcp",
            "description": "GitHub MCP — PRs, issues, code search, workflows, labels, releases. Copilot-managed auth."
        },
        "fetch": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-fetch@latest"
            ],
            "description": "HTTP client for testing dashboard data sources (Open-Meteo, Hebcal, Yahoo, CoinGecko). GET/POST with headers."
        },
        "filesystem": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-filesystem@latest",
                "${workspaceFolder}"
            ],
            "description": "Workspace-scoped file ops — coverage reports, test outputs, configs, build artifacts."
        },
        "gitkraken": {
            "type": "http",
            "url": "https://mcp.gitkraken.com/mcp",
            "description": "Git blame, log, diff, branch ops, PR workflow. Cross-repo MyScripts/ monorepo work."
        },
        "playwright": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "-y",
                "@anthropic/mcp-playwright@latest"
            ],
            "description": "Browser automation for E2E validation, screenshot capture, and visual regression verification in chat."
        }
    }
}
